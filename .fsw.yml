desc: Auto generated by fswatch [dockerized-go]
triggers:
- name: "source"
  pattens:
  - '**/*.go'
  env:
    DEBUG: "1"
  cmd: go install && $GOPATH/bin/dockerized-go
  shell: true
  delay: 100ms
  stop_timeout: 20s
  signal: TERM
  kill_signal: "SIGTERM"
watch_paths:
- .
watch_depth: 1000
